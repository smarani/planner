{% if latest_course_list %}
    <ul>
    {% for course in latest_course_list %}
        <li><a href="/planner/{{ course.id }}/">{{ course.major }}</a></li>
    {% endfor %}
    </ul>
{% else %}
    <p>No polls are available.</p>
{% endif %}
